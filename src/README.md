# Context Compression

Focus and compressed-context timeline panel for passive operational Grafana dashboards.

The panel renders every numeric time series Grafana passes to it, applies configurable adaptive bucket aggregation, projects time into historical, transition, and recent regions, and supports a zero-safe `log1p` Y-axis for count-heavy operational data.
